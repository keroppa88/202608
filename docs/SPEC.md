# 株価・指数データ収集 仕様書（暫定）

- 版: draft-11
- 作成: 2026-08-10
- 状態: **仮**。日本関連ソース（東証、日経の指数、JPX API）の調査が未着手のため確定ではない。
  日経の記事収集（§4A・§4B）と指数ページ（§4C）は実装・動作確認済み。
  ワークフロー4本とも実行スケジュール設定済み（§8）。

### このリポジトリの位置づけ

**取得したデータの倉庫**として運用する。ページとして公開・宣伝することは意図しない。

---

## 1. 目的

海外の主要株価指数・セクター指数・REIT指数・商品指数・為替・個別銘柄、および日本の
主要指数・銘柄について、**日次の四本値（始値・高値・安値・終値）を継続的に記録する**。

### 背景にある要件

当初はスプレッドシート上で `GOOGLEFINANCE` 関数を並べる方式を検討したが、以下の理由で採らない。

- 銘柄数が増えるほど再計算が重くなり、全体が一括で失敗する
- 取得に失敗しても**エラーにならず空欄になる**ため、更新不全に気づけない
- 成否の記録・再試行・通知ができない

したがって本仕様は **「1銘柄 = 1リクエスト、成否を個別に記録する」** ことを中核に置く。

---

## 2. 設計方針

### 2.1 生データを無加工で保存し、抽出は後段で行う

取得したレスポンスは**一切加工せずそのままファイルに保存**する。四本値の抽出は
保存済みファイルを入力とする別処理として実装する。

理由:

- 抽出ロジックを変更しても、**再取得なしで過去分をやり直せる**
- 配信側のフォーマットが変わっても生データが残るため、原因を追跡できる
- 「取得失敗」と「抽出失敗」を別々の事象として扱える

### 2.2 ウェブページは「見たままのテキスト」で扱う

人が画面を全選択してコピーしたものと同じテキストを丸ごと保存し、
**そのテキストだけを入力に抽出する**。タグ・クラス名・属性・XPath は一切参照しない。

判断の手がかりは、人が画面を見て使うのと同じものにする。

- 掲載日時は「2026年8月10日 15:48」という形で1行に現れる
- 題名はその近くにある、いちばん長い行
- 本文はその下に続く長い行の連なり

取得手段は2つあるが、得られるテキストは同じなので抽出側は区別しない（`src/page_text.py`）。

| 手段 | 用途 |
|---|---|
| `capture(url)` | ヘッドレスブラウザで開いて表示テキストを取る。JavaScript描画やボット判定のあるページ |
| `text_from_html(html)` | 取得済みHTMLを表示テキストに変換する。中身が静的HTMLに入っているページ |

例外は**リンク先URL**のみ。画面に表示されない情報なので HTML から拾う。
これは人がリンクをクリックする動作に相当し、記事の中身の解釈ではない。

### 2.3 銘柄ごとに独立して取得する

1銘柄の失敗が他の銘柄に波及しない構造とする。取得は銘柄単位でリトライし、
失敗した銘柄のみをエラーとして記録する。

### 2.4 「成功したように見える失敗」を必ず検知する

HTTP 200 が返っても中身が正しいとは限らない。以下を必須チェックとする。

| チェック | 内容 |
|---|---|
| 値の型 | 価格が数値であること（null・空文字を弾く） |
| 鮮度 | 取引所側タイムスタンプが一定日数以内であること |
| 銘柄同一性 | 配信元が返す銘柄名が期待と一致すること |

**§9 の検証記録に、実際にこの罠へ落ちた事例を残してある。**

---

## 3. データソース

### 3.1 採用（検証済み）

#### Yahoo Finance chart API

```
https://query1.finance.yahoo.com/v8/finance/chart/{symbol}?interval=1d&range=5d
```

- 認証不要。1リクエスト = 1銘柄
- レスポンスは JSON。`indicators.quote[0]` に四本値、`meta` に通貨・取引所・タイムスタンプ
- `meta.exchangeName` に配信元取引所コードが入る（`SNP` = S&P Dow Jones、`GER` = XETRA 等）
- **非公式API**。予告なく仕様変更・停止の可能性がある

#### MOEX ISS API（ロシアRTS）

```
現在値: https://iss.moex.com/iss/engines/stock/markets/index/securities/RTSI.json
履歴:   https://iss.moex.com/iss/history/engines/stock/markets/index/securities/RTSI.json?from=YYYY-MM-DD
```

- モスクワ取引所の公式データサービス。認証不要。ドキュメントは `https://iss.moex.com/iss/reference/`
- **履歴を期間指定で一括取得できる**ため、取得漏れを後から遡って復旧できる
- 注意: `iss.moex.com/robots.txt` は `Disallow: /`（§10 参照）

#### 日経「東証大引け」記事

```
検索:   https://www.nikkei.com/search?keyword=東証大引け、&volume=2
記事:   https://www.nikkei.com/article/{記事ID}/
```

- **静的HTMLに全て含まれる。ヘッドレスブラウザは不要**（curl 相当の取得で足りる）
- 検索ページ（約1MB）に記事IDとタイトルが出現順に含まれる
- 記事ページの JSON-LD（`application/ld+json`）に `headline` と `datePublished` が入る
- 本文は `<p class="paragraph_...">` として記事セクション（`data-track-article-content`）内に格納
- 掲載時刻は大引け（15:00）の約50分後。実測値は 15:48:21

詳細は §4A。

### 3.2 未調査（今後追加予定）

以下は**未着手**。URL・取得可否・フォーマットいずれも未確認。

| # | ソース | 対象 | 確認が必要な事項 |
|---|---|---|---|
| A | 東証（JPX）ページ | TOPIX 関連指数 | 対象指数の範囲（TOPIX / TOPIX500 / スタイル・サイズ別など）、取得URL、更新頻度、四本値の有無 |
| B | 日経ページ | 日経関連**指数** | 対象指数の範囲（日経平均 / JPX日経400 / 業種別など）、取得URL、四本値の有無。※「東証大引け」記事の収集は調査済み（§4A） |
| C | **JPX API（有料・契約済み）** | 日本の主要株価 | 契約プラン名、エンドポイント、認証方式、レート制限、**再配布可否** |

C については、認証情報を GitHub Secrets に格納する必要がある。
また**有料データの公開リポジトリへの掲載可否**を契約条件で確認すること（§10）。

### 3.3 不採用（判断済み）

| ソース | 不採用の理由 |
|---|---|
| Google 検索ページ | HTMLに数値が含まれない（JavaScript描画）。HTTP 200・92KBが返るが `株価` の語すら0件。robots.txt が `Disallow: /search`。`sxsrf` パラメータは使い捨てトークン |
| 時事通信API（`rls.jiji.com/mumshp/d/*`） | MUFGサイトの裏側で動く非公開API。**Referer チェックがあり、偽装しないと接続を切られる**。第三者利用が想定されていない。四本値は取得できたが、公開リポジトリで運用する前提と整合しないため除外 |
| Stooq | この環境から 404。地域制限の可能性 |
| exchangerate.host | 無料枠が廃止され API キー必須に |

---

## 4. 銘柄一覧（89 件）

全件、四本値の取得と鮮度を実測確認済み（2026-08-10 時点）。
機械可読な一覧は [`symbols.csv`](../symbols.csv) を参照。

### 4.1 海外指数（15）

| シンボル | 名称 | 通貨 |
|---|---|---|
| `^GSPC` | S&P500 | USD |
| `^RUT` | ラッセル2000 | USD |
| `^GDAXI` | ドイツDAX | EUR |
| `^FTSE` | 英国FTSE100 | GBP |
| `^FCHI` | 仏CAC40 | EUR |
| `^STOXX50E` | ユーロ・ストックス50 | EUR |
| `FTSEMIB.MI` | イタリア40 | EUR |
| `XU100.IS` | イスタンブール100 | TRY |
| `399001.SZ` | 深セン成分指数 | CNY |
| `^HSI` | 香港ハンセン | HKD |
| `^SET.BK` | タイSET | THB |
| `^BSESN` | インドSENSEX30 | INR |
| `^KS11` | 韓国総合株価指数 | KRW |
| `^TWII` | 台湾加権 | TWD |
| `^SOX` | SOX（フィラデルフィア半導体） | USD |

### 4.2 ロシアRTS（1・MOEX ISS）

| コード | 名称 | 通貨 |
|---|---|---|
| `RTSI` | ロシアRTS指数 | USD |

### 4.3 S&P500 セクター（11）

| シンボル | 名称 |
|---|---|
| `^SP500-30` | 生活必需品 |
| `^GSPE` | エネルギー |
| `^SP500-40` | 金融 |
| `^SP500-35` | ヘルスケア |
| `^SP500-20` | 資本財 |
| `^SP500-45` | IT |
| `^SP500-15` | 素材 |
| `^SP500-60` | 不動産 |
| `^SP500-50` | 電気通信 |
| `^SP500-55` | 公益 |
| `^SP500-25` | 一般消費財・サービス |

> エネルギーのみ命名規則から外れる。`^SP500-10` は存在せず `^GSPE` が正しい。

### 4.4 米国REIT（5・Dow Jones 体系で統一）

| シンボル | 名称 | 対象 |
|---|---|---|
| `^REI` | Dow Jones Equity All REIT | 総合 |
| `^DJUSIO` | DJ U.S. Industrial & Office REITs | オフィス・物流 |
| `^DJUSRL` | DJ U.S. Retail REITs | 商業（小売・モール） |
| `^DJUSRN` | DJ U.S. Residential REITs | 住宅・賃貸 |
| `^DJUSHL` | DJ U.S. Hotel & Lodging REITs | ホテル |

> 全て同一体系のため、総合とセクターの水準・変化率を直接比較できる。

### 4.5 商品・ETF（3）

| シンボル | 名称 | 備考 |
|---|---|---|
| `GC=F` | 金（COMEX先物） | 期近物のため限月交代時に価格が不連続になる |
| `^SPGSIN` | S&P GSCI Industrial Metals | 工業用金属指数 |
| `ARKK` | ARK Innovation ETF | |

### 4.6 為替（14・すべて対円）

| シンボル | 通貨ペア | | シンボル | 通貨ペア |
|---|---|---|---|---|
| `USDJPY=X` | 米ドル | | `INRJPY=X` | インドルピー |
| `EURJPY=X` | ユーロ | | `TRYJPY=X` | トルコリラ |
| `GBPJPY=X` | 英ポンド | | `BRLJPY=X` | ブラジルレアル |
| `AUDJPY=X` | 豪ドル | | `MXNJPY=X` | メキシコペソ |
| `CHFJPY=X` | スイスフラン | | `ZARJPY=X` | 南アフリカランド |
| `CNYJPY=X` | 中国元 | | `KRWJPY=X` | 韓国ウォン |
| `HKDJPY=X` | 香港ドル | | `TWDJPY=X` | 台湾ドル |

> 元・香港ドル・ウォン・台湾ドルは、現地通貨で記録している個別株
> （テンセント・サムスン・TSMC 等）と対応している。

### 4.7 個別銘柄（37）

**米国・テック（16）**

`AAPL` `MSFT` `GOOGL` `AMZN` `META` `NVDA` `TSLA` `PLTR` `MU` `ORCL` `AMD` `INTC`
`ARM` `AVGO` `QCOM` `SNDK`

> `SNDK` = Sandisk Corporation。旧親会社 `WDC`（Western Digital）とは別銘柄。

**米国・金融/小売（5）**

`BRK-B` `JPM` `GS` `WMT` `MCD`

**アジア（9）**

| シンボル | 名称 | 市場 |
|---|---|---|
| `005930.KS` | サムスン電子 | 韓国 |
| `000660.KS` | SKハイニクス | 韓国 |
| `002371.SZ` | NAURA（北方華創） | 深セン |
| `0981.HK` | SMIC | 香港 |
| `1810.HK` | Xiaomi | 香港 |
| `0700.HK` | テンセント | 香港 |
| `1211.HK` | BYD | 香港 |
| `9988.HK` | アリババ | 香港 |
| `2330.TW` | TSMC | 台湾 |

**欧州（7）**

| シンボル | 名称 | 市場 | 注意 |
|---|---|---|---|
| `SAP.DE` | SAP | XETRA | |
| `VOW3.DE` | フォルクスワーゲン | XETRA | 優先株。普通株は `VOW.DE` |
| `BAS.DE` | BASF | XETRA | |
| `HSBA.L` | HSBC | ロンドン | **GBp（ペンス）建て**。ポンドではない |
| `BNP.PA` | BNPパリバ | パリ | |
| `MC.PA` | LVMH | パリ | |
| `ASML.AS` | ASML | アムステルダム | |

> **7銘柄は米国市場でも取得できるが、現地上場を採る**（確定）。本来の株価で記録するため。
> 内訳は ADR/ADS が6件（`TCEHY` `BABA` `SAP` `LVMUY` `TSM` `HSBC`）と、
> ASML の**米国登録株**（NY Registry Share）1件。ASML の Nasdaq 上場は預託証券ではなく
> 原株を表す登録株であり、他の6件とは種類が異なる。
> 通貨が市場ごとに異なる点にも注意。特に `HSBA.L` は GBp（ペンス）建て。

### 4.9 暗号資産（3・ドル建て）

| シンボル | 名称 |
|---|---|
| `BTC-USD` | ビットコイン |
| `ETH-USD` | イーサリアム |
| `XRP-USD` | リップル |

**24時間365日動くため、他と日付の扱いが異なる。**

- 日足の区切りは UTC 0時。JST 07:00 の実行時点では当日分が未確定なので、
  **前日分（UTC基準）が確定足として記録される**
- 株式市場が休みの土日にも行が増える
- 判定は既存の仕組みがそのまま働く。`currentTradingPeriod` の
  `regular`（UTC 00:00〜23:59）を過ぎているかで確定/未確定を分けている（§2.4）

### 4.8 日本関連（未定）

§3.2 の A・B・C の調査結果を反映して追記する。記事収集は §4A。

---

## 4A. 日経「東証大引け」記事の収集

### 目的

平日の大引け後に掲載される「東証大引け」記事を、**本文ごと蓄積する**。
株価の数値そのものは §4 の各ソースから別途取得しているため、
ここで欲しいのは**記事の文章**である。

### 保存先

```
article/
├── japan202608.txt
├── japan202609.txt
└── japan202610.txt   …以降、年月ごとに1ファイル
```

- ファイル名は `japan{YYYY}{MM}.txt`
- 振り分けは**記事の掲載日（記事年月日）基準**。月末深夜に取得がずれ込んでも、
  8月31日の記事は `japan202608.txt` に入る
- 追記は時系列順（新しいものを末尾に追加）
- 文字コードは UTF-8、改行は LF

### レコード形式

```
================================================================================
題名      : 東証大引け　日経平均は3日ぶり反発　TOPIX最高値接近　米利上げ観測後退が追い風
記事年月日: 2026-08-10 15:48:21 JST
取得年月日: 2026-08-10 17:05:12 JST
URL       : https://www.nikkei.com/article/DGXZQOFL102V60Q6A810C2000000/
--------------------------------------------------------------------------------
10日の東京株式市場で日経平均株価は3営業日ぶりに反発し、終値は前週末比1363円51銭
（2.08%）高の6万6970円22銭だった。…

（以下、段落を空行区切りで全文）
```

保持する項目は4つ。

| 項目 | 取得元 |
|---|---|
| 題名 | JSON-LD `headline` |
| 記事年月日 | JSON-LD `datePublished` |
| 記事本文 | 記事セクション内の段落を順に連結 |
| 取得年月日 | 実行時のシステム時刻 |

URL は再取得・重複判定のために併記する。

### 処理フロー

```
1. 検索ページを丸ごと保存                     ← 生HTML
2. 保存済みHTMLから記事IDとタイトルを全件抽出
3. 「東証大引け」で始まるもののうち、掲載日時が最新の1件を選ぶ
4. その記事ページを丸ごと保存                 ← 生HTML
5. 保存済みHTMLから 題名・記事年月日・本文 を抽出
6. 取得年月日を付けて article/japan{YYYYMM}.txt に追記
```

取得（1・4）と抽出（2・5）を分離し、抽出ロジックを変更しても再取得を不要とする（§2.1）。

**「一番上を取る」ではなく掲載日時で判定する。** 並び順に依存すると、
日経側が順序を変えた際に別の日の記事を静かに拾い続けることになり、
§2.4 で排除している「エラーにならない失敗」に該当する。

### 重複防止

**記事IDで判定する。** すでに同じIDが当月ファイルに存在する場合は追記しない。

これにより以下が自動的に処理される。

- 日本の祝日 — 記事が出ないため検索結果は前営業日のものになる。ID重複で追記されない
- ワークフローの再実行 — 二重追記されない
- cron の遅延・複数回起動 — 同上

「祝日カレンダーを持って判定する」方式は採らない。休場日の変更に追随できないため。

### 実行時刻

```
cron: "0 9 * * 1-5"     # UTC 月〜金 09:00 = JST 月〜金 18:00
```

掲載実測が 15:48 のため、JST 18:00 に余裕を持って起動する。
JST 18:00 は UTC では同日 09:00 なので、曜日はずれない（§8 の一覧参照）。
GitHub Actions の cron は数十分遅れることがあるが、記事は掲載済みのため影響しない。

祝日にも起動するが、上記の重複判定により**何も追記せず正常終了**する。
これは失敗ではないため、ワークフローを fail させない。

---

## 4B. 日経「米国株」大引け記事の収集

### 検索

```
https://www.nikkei.com/search?keyword=米国株、　NQNニューヨーク　米株式市場でダウ工業株30種平均は&volume=10
```

§4A と同じく静的HTMLで題名・掲載日時・本文が取得できる（実測 9段落・1,194文字）。

保存先は `article/us{YYYYMM}.txt`。形式・月ファイルの振り分け規則は §4A と同一。

### この検索は1営業日につき3本ヒットする

キーワードが**本文**に一致するため、東証大引け（題名一致）と違い3種類とも拾ってしまう。

| 掲載時刻(JST) | 題名の特徴 | 本文の「終値」 | 種別 |
|---|---|---|---|
| 22:30頃 | 「…で始まる」 | なし | 寄り付き |
| 05:07頃 | 値幅の記載なし | あり | 引け速報 |
| **05:36頃** | **「◯◯ドル高 / ◯◯ドル安」** | あり | **大引け ← 対象** |

実測10件すべてでこの規則が成立した（151ドル高 / 464ドル安 / 263ドル高 / 907ドル高）。

### 選択ロジック

```
1. 掲載日時の新しい順に候補を見る
2. 題名が「数字＋ドル高／ドル安」を含む
3. かつ本文に「終値」を含む
4. 最初に一致したものを採用
5. 10件見て該当しなければエラー（黙って別の記事を入れない）
```

**位置だけに頼らない。** JST 22:30 を過ぎると検索結果の先頭が寄り付き記事に変わるため、
「一番上」で取ると実行時刻の変更や遅延で静かに別種の記事を拾い続けることになる。

### 実行スケジュール（確定）

大引け記事はまず速報的に掲載され、**5時台から7時の間に最終版になる**。
必要なのは各日の最終版のみ。**JST 07:00** に起動する（§8 の注意点も参照）。

```
cron: "0 22 * * 1-5"     # UTC 月〜金 22:00 = JST 火〜土 07:00
```

米国市場は月〜金に取引され、その大引け記事は翌朝 JST に掲載される。
したがって日本時間では**火曜の朝から土曜の朝**が稼働日となる。

**cron は UTC で解釈されるため、曜日の指定に注意が必要。**
JST 08:00 は UTC では前日 23:00 にあたるので、
「JST 火〜土」を表すには UTC 側で `1-5`（月〜金）と書く。

| UTC | JST |
|---|---|
| 月 22:00 | 火 07:00 |
| 火 22:00 | 水 07:00 |
| 水 22:00 | 木 07:00 |
| 木 22:00 | 金 07:00 |
| 金 22:00 | **土 07:00** |

> 注意: 重複判定は記事IDで行うため、**一度取得するとその版で確定する**
> （後から最終版が出ても記事IDが同じならスキップされる）。1日1回の起動を前提とする。
> cron の遅延は最終版の確定を待つ方向に働くため、遅れる分には問題ない。

米国市場の休場日（感謝祭など）には記事が出ないため、検索結果は前営業日のものになる。
記事IDの重複判定により**何も追記せず正常終了**する（§4A と同じ扱い）。

### 取引日と掲載日のずれ

米国 8月7日（金）の取引は **8月8日（土）05:36 JST** に掲載される。
月ファイルの振り分けは掲載日基準のため、米国 8月31日の取引は `us202609.txt` に入る。

---

## 4C. 日経の指数ページ（indexes.nikkei.co.jp）

### 状況

ご指定の各ページは **Cloudflare のボット判定**が入っており、通常のHTTP取得では
403（`Just a moment...`）になる。ブラウザ相当のヘッダを付けても通らない。

唯一 `nkave/historical/nikkei_stock_average_daily_jp.csv` だけは取得できるが、

- 日経平均の四本値のみ。**時刻・寄与度は含まれない**
- 末尾に「本資料は日経の著作物であり、…無断で複写、複製、転載または流布することが
  できません」という明示の禁止条項がある
- 四本値自体は Yahoo `^N225` で取得済みのため、この経路を使う利点がない

したがって**このCSVは使わない**。

### 方針

ヘッドレスブラウザでページを開き、**画面に見えているテキストを丸ごと保存する**。

- HTML ではなく表示テキスト（全選択してコピーしたものに相当）
- XPath・CSSセレクタによる位置指定をしない（§2.2）
- リンクを辿らない。指定URLを開くだけ
- 抽出は保存済みテキストを入力に、別処理として実装する（§2.1）

### 取得対象

| key | 内容 | 欲しい項目 |
|---|---|---|
| `nk225_summary` | 日経平均 サマリー | 四本値・その時刻・寄与度など詳細すべて |
| `nk225_profile` | 日経平均 プロフィル | 同上 |
| `nkscd_profile` | 日経半導体株指数 | 四本値・時刻 |
| `index_list` | 指数一覧 | カバードコール／同ATM／内需株50／外需株50 の大引け値 |

日経VI の `idx` は未確定。`index_list` を取得すれば判明する。

### 実行方法

`.github/workflows/nikkei-index.yml` を **手動実行のみ**（`workflow_dispatch`）で用意した。
Actions の「Run workflow」から起動する。

```
pip install playwright && playwright install --with-deps chromium
python3 src/fetch_nikkei_index.py
```

**Cloudflare はデータセンターIPからも通過できた**（手動実行で確認）。
1日1回の頻度であれば問題ないと判断し、JST 月〜金 18:00 の定時実行に載せた。

### 出力

| ファイル | 内容 |
|---|---|
| `data/nikkei_ohlc.csv` | 四本値。大引け値のみの指数は終値だけ入り、他は空欄 |
| `data/nikkei_ohlc_time.csv` | 四本値がついた時刻 |
| `data/nikkei225_detail.csv` | 日経平均の詳細。項目数が多く列が揃わないため長形式 |

抽出は保存済みの表示テキストだけを入力に行う。手がかりは
タブ区切りの `始値\t65,905.10\t09:00` 行、指数一覧の4行1組、
寄与度の「◯◯円」で終わる行の直前にあるセクター名。

Cloudflare のチャレンジは接続元IPで難易度が変わる。自宅回線からはほぼ通るが、
**GitHub Actions のデータセンターIPからは弾かれる可能性が高い**。
まず手元で実行して通ることを確認し、そのうえで実行場所を決める。

保存されたテキストが得られてから抽出を実装する。ページを一度も表示できていない
現状では、どこに何が書かれているか分からず抽出ロジックを書けない。

---

## 5. ディレクトリ構成（案）

```
.
├── .github/
│   ├── workflows/
│   │   ├── market.yml         相場データ
│   │   ├── nikkei-jp.yml      東証大引け記事
│   │   └── nikkei-us.yml      米国株記事
│   └── scripts/commit.sh      取得結果のコミット・push（競合時は rebase 再試行）
├── symbols.csv                銘柄マスタ
├── src/
│   ├── common.py              取得・保存・時刻の共通処理
│   ├── fetch_market.py        相場データ（§4）
│   ├── nikkei.py              記事収集の共通処理（§4A / §4B）
│   ├── fetch_nikkei_jp.py     東証大引け記事（§4A）
│   └── fetch_nikkei_us.py     米国株大引け記事（§4B）
├── data/
│   ├── raw/YYYY-MM-DD/        生レスポンス（無加工・gzip）
│   │   ├── yahoo_TSLA.json.gz
│   │   ├── moex_RTSI.json.gz
│   │   ├── nikkei_jp_search.html.gz
│   │   └── nikkei_jp_DGXZQ….html.gz
│   └── overseas.csv           相場データ（全銘柄・全日付）
├── article/
│   ├── japan202608.txt        東証大引け記事（§4A）
│   └── us202608.txt           米国株大引け記事（§4B）
└── docs/
    └── SPEC.md                本書
```

Python は標準ライブラリのみを使用し、依存パッケージを持たない。

生データは gzip で保存する。可逆であり、`common.load_raw()` で読み戻して
再抽出できるため、§2.1 の方針は変わらない。無圧縮では 1日 2.75MB、
gzip で 0.65MB になる。

---

## 6. 出力形式

相場データは **`data/overseas.csv` の1ファイル**にまとめる。**1行 = 1銘柄の1日**（長形式）。

| 列 | 内容 |
|---|---|
| `trade_date` | 取引日。**取引所ローカルの日付**（UTC に直すと市場ごとに1日ずれる） |
| `category` | 分類（海外指数 / セクター / REIT / 商品 / ETF / 為替 / 個別株） |
| `name` | 表示名 |
| `symbol` | シンボル |
| `source` | データソース（`yahoo` / `moex`） |
| `open` / `high` / `low` / `close` | 四本値 |
| `volume` | 出来高。取れない銘柄は**空欄**（0 とは区別する） |
| `currency` | 通貨 |
| `exchange` | 配信元取引所コード |
| `fetched_at` | 取得実行時刻（JST, ISO8601） |

### 銘柄ごとにファイルを分けない理由

銘柄を追加・削除してもファイル構成が変わらず、読み込みが1回で済み、
同じ日の市場横断比較がそのままできる。年間約19,000行（2〜3MB）で、
10年貯めても表計算ソフトの行上限に届かない。
後から銘柄ごとに分割するのは容易だが、逆は手間がかかる。

日付を行・銘柄を列にする横形式は採らない。銘柄を1つ足すだけで列構成が変わり、
過去の行と噛み合わなくなるため。

### 毎回、取得できた確定足をすべて取り込む

Yahoo は1か月分、MOEX は14日分の足を返すので、最新1本だけでなく**全部**を
`(trade_date, symbol)` をキーに追記する。これにより:

- 取得を1日飛ばしても、次の実行で自動的に埋まる
- 休場日はそもそも足が存在しないため、**「更新なし」と「取り逃し」を区別する必要がない**
- 国ごとに休場日が違っても、追加の判定を持たずに済む

### 注意

- **取引日は銘柄ごとに異なる。** 実行時刻によって、欧州・アジアは当日、
  米国は前営業日になる。日付列を必ず参照すること
- 出来高 0 を異常値として弾かないこと（指数では正常）
- **進行中のセッションは記録しない。** 取引時間中に叩くと Yahoo は未確定の足も
  四本値が埋まった状態で返すが、その `close` は終値ではない（§9 参照）

---

## 7. 失敗検知

| 事象 | 検知方法 | 動作 |
|---|---|---|
| HTTP エラー | ステータスコード | 指数バックオフで最大3回リトライ |
| 価格が null / 非数値 | 型チェック | 当該銘柄をエラーとして記録 |
| 時系列が空 | `timestamp` の有無 | 配信停止の疑いとして記録 |
| 値が古い | 取引所タイムスタンプと現在日時の差 | 閾値超過でエラー |
| 銘柄の取り違え | `longName` と期待名の突合 | 初回登録時および定期的に目視確認 |

エラーが1件でもあればワークフローを **fail** させ、GitHub の通知経路で気づけるようにする。

**日次コミットの有無そのものが実行の証跡になる。**
コミットが無い日＝処理が走らなかった日、と判断できる。

---

## 8. 実行環境

- GitHub Actions（`schedule` トリガー）
- リポジトリ: `keroppa88/202608`（**公開**）

### スケジュール一覧

**cron は UTC で解釈される。** JST の朝に動かすものは UTC では前日の夜になるため、
曜日の指定が1日ずれる。下表の対応で設定済み。

**朝の部（JST 07:00）** — 海外市場が閉じた後にまとめて取る

| ワークフロー | cron（UTC） | 起動（JST） | 内容 |
|---|---|---|---|
| `market.yml` | `0 22 * * 1-5` | **火〜土 07:00** | 海外指数・為替・セクター・REIT・商品・個別株（77件） |
| `nikkei-us.yml` | `0 22 * * 1-5` | **火〜土 07:00** | 日経「米国株」大引け記事 |

**夕の部（JST 18:00）** — 東京市場が閉じた後にまとめて取る

| ワークフロー | cron（UTC） | 起動（JST） | 内容 |
|---|---|---|---|
| `nikkei-jp.yml` | `0 9 * * 1-5` | **月〜金 18:00** | 日経「東証大引け」記事 |
| `nikkei-index.yml` | `0 9 * * 1-5` | **月〜金 18:00** | 日経の指数ページ（§4C） |
| （予定） | | 月〜金 18:00 | JPX・東証関連（§3.2-A / §3.2-C） |

朝の部を JST 火〜土に置く理由は、その時点で
米国・欧州・アジアのいずれも**前営業日の引けが確定している**ため。
月曜朝は新しい確定足がないので対象外とする。

> **米国株記事のみ注意点がある。** 最終版が固まるのは5時台〜7時であり（§4B）、
> 07:00 起動はその境界にあたる。GitHub Actions の cron は数分〜数十分遅れる傾向があり
> 実際には 07:05〜07:30 頃の起動になるため実用上は問題ないが、
> **確実性を優先するなら 07:30 起動が安全**。
> 重複判定は記事IDで行うため、一度取得すると後から最終版が出ても上書きされない。

### concurrency グループは使わない

3本とも同じブランチへ push するが、`concurrency` による直列化は行わない。

同一グループで待機中の run が2件以上になると、**古い方が自動でキャンセルされる**。
取得が1回まるごと消えるうえ、失敗としても記録されない。
§2.4 で排除している「エラーにならない失敗」そのものになる。

代わりに `.github/scripts/commit.sh` が push 競合時に rebase して最大5回再試行する。
朝の部は2本が同時刻に起動するため、`commit.sh` の再試行がここで効く。

### 制約

| 項目 | 内容 |
|---|---|
| cron の精度 | 数分〜数十分の遅延がある。混雑時はスキップされ得る |
| 60日ルール | リポジトリが60日間無活動だとスケジュールが自動停止する。日次コミットにより回避される |
| 実行時間 | 相場データ77銘柄で2〜3分。記事は各1分未満 |

---

## 9. 検証記録（2026-08-10 実施）

**「値が返る = 正しい」ではない**ことを示す実例。同種の確認を新規銘柄追加時にも行うこと。

| シンボル | 症状 | 判明したこと |
|---|---|---|
| `RTSI.ME` | 1,144.79 が返る | `regularMarketTime` が **2022-07-08**。4年前で配信停止 |
| `^RMZ` | 1,289.55 が返る | 最終更新 **2019-12-06**。MSCI US REIT指数は既に移行済み |
| `^BCOMIN` | 96.49 が返る | 最終更新 **2020-05-28** |
| `^TPX` | 105.18 が返る | TOPIX（約4,100）と無関係の別商品 |
| `1597.T` | 1,869 が返る | TOPIX500グロースを期待したが **MAXIS J-REIT ETF** |
| `1598.T` | 21,580 が返る | **NEXT FUNDS Russell/Nomura Fundamental Index** |
| `1599.T` | 38,020 が返る | **Daiwa ETF JPX-Nikkei 400** |
| Google検索 | HTTP 200・92KB | 本文に `株価` `TSLA` が **0件**。JavaScript描画のため空 |
| 取引時間中の Yahoo | 四本値が揃って返る | **進行中セッションの足**。`close` は終値ではなく現在値で、1分後には別の値になる |
| `range=5d` の指数 | 足が1本だけ返る | S&P500セクター等は 5d だと1本。それが進行中の足だと確定足が0本になる。`1mo` で回避 |

いずれも**エラーにならない**。`try/catch` や `if (status === 200)` では検知できない。

### 相互検証の例

ロシアRTS（2026-08-07）について、独立した2ソースが完全一致した。

| ソース | 始値 | 高値 | 安値 | 終値 |
|---|---|---|---|---|
| MOEX ISS | 884.56 | 894.05 | 871.69 | 874.64 |
| 時事API（不採用） | 884.56 | 894.05 | 871.69 | 874.64 |

---

## 10. ライセンス・公開に関する留意事項

本リポジトリは**公開**である。ページとして公開・宣伝する意図はなく、
取得データの倉庫として運用する方針で確定済み。以下は認識した上で運用する。

- Yahoo Finance は非公式APIであり、利用が明示的に許諾されているわけではない
- `iss.moex.com/robots.txt` は `Disallow: /`。ただし MOEX 自身が
  ISS を公開ドキュメント付きのデータサービスとして提供している
- 株価指数の指数値は本来ライセンス商品であり、再配布には制約があり得る
- 日経の記事本文は著作物である。`nikkei.com/robots.txt` は汎用の `User-agent: *`
  に対して `/article/` `/search` を禁じていないが、AI・スクレイピング系の
  エージェント（`ClaudeBot`、`GPTBot`、`Scrapy` など40件超）を名指しで全面拒否しており、
  データ利用は問い合わせ窓口（`nkbb.nikkei.co.jp`）へ案内している

### JPX API（有料）について — 要確認

**契約済みの有料データを公開リポジトリにコミットする場合、再配布にあたる可能性がある。**
利用規約を確認し、必要であれば以下のいずれかを採る。

1. 当該データのみリポジトリにコミットせず、Actions の Artifacts に保存する
2. リポジトリを非公開に変更する
3. 契約上問題ないことを確認した上でコミットする

認証情報は必ず **GitHub Secrets** に格納し、コードやログに出力しない。

---

## 11. 未確定事項

| # | 項目 | 内容 |
|---|---|---|
| 1 | ~~ADR / 現地上場の統一~~ | **現地上場で確定。** 米国でも取得できる7銘柄（テンセント・アリババ・SAP・LVMH・ASML・TSMC・HSBC）はすべて現地市場の価格を採る。米国側は ADR/ADS が6件、ASML のみ米国登録株。通貨は市場ごとに異なる（§4.7 の注意書きを参照） |
| 2 | 東証ページ | 対象とする TOPIX 関連指数の範囲、取得URL |
| 3 | 日経ページ（指数） | 対象とする日経関連指数の範囲、取得URL。※記事収集は §4A で確定済み |
| 4 | JPX API | 契約プラン、エンドポイント、認証方式、レート制限 |
| 5 | ~~実行スケジュール~~ | **3本とも設定済み（§8）。** 朝の部 火〜土 07:00（相場データ・米国株記事）/ 夕の部 月〜金 18:00（東証大引け記事）。未実装の JPX・東証関連と日経数値関連も夕の部に入れる予定 |
| 6 | 履歴の初期投入 | MOEX ISS は履歴を遡れる。Yahoo も `range` 指定で過去分を取得可能。開始時点で何年分を入れるか |

---

## 12. 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| draft-1 | 2026-08-10 | 初版。海外分の調査結果を記載。日本関連ソースは未着手 |
| draft-2 | 2026-08-10 | 日経「東証大引け」記事の収集仕様（§4A）を追加。静的HTMLで本文まで取得可能なことを確認し、ヘッドレスブラウザ不要と判明。リポジトリの位置づけ（取得データの倉庫）を明記 |
| draft-3 | 2026-08-10 | 日経「米国株」大引け記事（§4B）を追加。3プログラムを実装し、全件の動作を確認。生データを gzip 保存に変更。米国株記事のスケジュールを JST 火〜土 08:00 に確定。他2本は手動実行のみ |
| draft-4 | 2026-08-10 | 残り2本のスケジュールを設定（§8）。concurrency グループを廃止し、push 競合は commit.sh の rebase 再試行で処理する方針に変更 |
| draft-5 | 2026-08-10 | スケジュールを朝の部（火〜土 07:00）と夕の部（月〜金 18:00）の2枠に整理。未実装の JPX・東証関連と日経数値関連の枠も記載 |
| draft-6 | 2026-08-10 | 日経の指数ページ（§4C）を追加。Cloudflare のため取得はヘッドレスブラウザで表示テキストを保存する方式とした。通過可否は未検証 |
| draft-7 | 2026-08-11 | ウェブページの扱いを「見たままのテキスト」に統一（§2.2）。記事の抽出が JSON-LD と `<p class>` に依存していたのを、テキストだけの処理に置き換え。生データも表示テキストで保存する形に変更 |
| draft-8 | 2026-08-11 | 日経の指数ページ用に手動実行ワークフローを追加（§4C）。日経記事の取得方法は現状維持（HTTP＋表示テキスト変換）で確定 |
| draft-9 | 2026-08-11 | 日経の指数ページが Cloudflare を通過できることを確認し、JST 月〜金 18:00 の定時実行に変更。抽出を実装し3ファイルに出力 |
| draft-10 | 2026-08-11 | ADR と現地上場の方針を現地上場で確定。日経VI（`idx=nk225vi`）の取得と3ファイル出力を実データで確認 |
| draft-11 | 2026-08-11 | 為替を5→14通貨ペアに拡張し、暗号資産3件（ドル建て）を追加。計89銘柄 |
| draft-6 | 2026-08-10 | 相場データの出力を `data/overseas.csv` の1ファイル・長形式に変更（§6）。毎回すべての確定足を取り込む方式にし、休場日と取り逃しの区別を不要化。進行中セッションを確定足として記録する不具合を修正。米国株記事は 08:00 JST に変更 |
